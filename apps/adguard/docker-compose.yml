services:
  adguard:
    image: adguard/adguardhome:latest
    container_name: adguard
    restart: always
    ports:
      - "53:53/udp"
      - "53:53/tcp"
      - "3000:3000/tcp"    # 초기 설정 UI
      - "13080:13080/tcp"  # 설정 완료 후 관리 UI
    volumes:
      - ./data/conf:/opt/adguardhome/conf
      - ./data/work:/opt/adguardhome/work
