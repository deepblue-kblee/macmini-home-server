user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    # 파일 업로드 크기 제한 (기본값 1m, 필요시 조정)
    client_max_body_size 50m;

    # 네트워크 3대 원칙: IP 대역별 신분 식별 (내부인=1, 외부인=0)
    # Cloudflare 경유 시 real_ip 복원 후 기준이 아닌, 원본 $remote_addr 기준으로 평가됨 — 직접 접속(내부망)에서만 신뢰할 것
    geo $remote_addr $is_internal {
        default        0;
        127.0.0.1      1;
        192.168.0.0/16 1;
        100.64.0.0/10  1;
        172.16.0.0/12  1;  # Docker 브리지 대역 (외부에서 절대 접근 불가한 사설 대역)
    }

    # gzip 압축
    # gzip on;
    # gzip_types text/plain text/css application/json application/javascript;

    # 로그 포맷 커스텀
    # log_format main '$remote_addr - $remote_user [$time_local] "$request" '
    #                 '$status $body_bytes_sent "$http_referer" '
    #                 '"$http_user_agent"';

    include /etc/nginx/conf.d/*.conf;
}
